{
  "scenario": "db_migration_lock",
  "alert": {
    "labels": {
      "alertname": "SlowQueries",
      "service": "orders",
      "severity": "critical",
      "scenario": "db_migration_lock"
    },
    "annotations": {
      "summary": "Orders p99 query latency above 4s; lock waits climbing",
      "description": "pg_stat_activity shows ACCESS EXCLUSIVE lock on orders table held by a CREATE INDEX statement; order list queries queueing behind it."
    },
    "fingerprint": "fp-migration-lock-001"
  },
  "deploys": [
    {
      "service": "orders",
      "commit_shas": ["b2c3d4e5f6"],
      "minutes_before_alert": 40
    }
  ]
}
